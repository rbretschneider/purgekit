<UserControl x:Class="PurgeKit.UI.Views.ProgramsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:conv="clr-namespace:PurgeKit.UI.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBoolConverter x:Key="InverseBool" />
    </UserControl.Resources>

    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Installed Programs" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="{Binding StatusText}" Opacity="0.7" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Progress -->
        <ui:ProgressBar Grid.Row="1" IsIndeterminate="True" Margin="0,0,0,8"
                        Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}" />

        <!-- Search and Drive filter -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ui:ControlHelper.PlaceholderText="Search by name or publisher..."
                     Width="300" Margin="0,0,16,0" />
            <TextBlock Text="Drive:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox ItemsSource="{Binding AvailableDrives}" SelectedItem="{Binding DriveFilter}" Width="80" Margin="0,0,16,0" />
            <TextBlock Text="Click any column header to sort" VerticalAlignment="Center" Opacity="0.4" FontSize="12" />
        </StackPanel>

        <!-- Programs List -->
        <ListView Grid.Row="3" ItemsSource="{Binding Programs}"
                  GridViewColumnHeader.Click="ColumnHeader_Click">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="240">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Name" Content="Program" />
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="160">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Publisher" Content="Publisher" />
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Publisher}" TextTrimming="CharacterEllipsis" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="100">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="InstallDate" Content="Installed" />
                        </GridViewColumn.Header>
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="InstallDateFormatted" />
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                    <GridViewColumn Width="110">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="LastUsed" Content="Last Used" />
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LastUsedFormatted}"
                                           ToolTip="{Binding LastUsedSource, StringFormat='Date sourced from {0}'}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="80">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Size" Content="Size" />
                        </GridViewColumn.Header>
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="SizeFormatted" />
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                    <GridViewColumn Width="55">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Tag="Drive" Content="Drive" />
                        </GridViewColumn.Header>
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="InstallDrive" />
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                    <GridViewColumn Header="" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Uninstall" Command="{Binding UninstallCommand}"
                                            Visibility="{Binding HasUninstaller, Converter={StaticResource BoolToVis}}"
                                            Style="{StaticResource DeleteButtonStyle}" />
                                    <StackPanel Visibility="{Binding HasUninstaller, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}">
                                        <TextBlock Text="No uninstaller" Opacity="0.5" FontSize="11" />
                                        <Button Content="Open Folder" Command="{Binding OpenInstallFolderCommand}"
                                                Style="{StaticResource SecondaryButtonStyle}" Margin="0,2,0,0" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Empty state -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVis}}"
                    IsHitTestVisible="False">
            <TextBlock Text="&#xE74C;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" FontSize="48"
                       HorizontalAlignment="Center" Opacity="0.3" />
            <TextBlock Text="No programs listed" FontSize="16" HorizontalAlignment="Center" Opacity="0.5" Margin="0,8,0,0" />
            <TextBlock Text="Click Scan All in the sidebar to list installed programs" FontSize="13" HorizontalAlignment="Center" Opacity="0.4" />
        </StackPanel>
    </Grid>
</UserControl>
